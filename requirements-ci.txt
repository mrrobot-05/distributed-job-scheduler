# CI/CD tool dependencies (pinned versions)
# These are NOT runtime dependencies - they are only used in CI
# Install with: pip install -r requirements-ci.txt

# Linting
ruff==0.11.13

# Type checking
mypy==2.3.1
django-stubs==5.2.9
django-stubs-ext>=5.2.9
djangorestframework-stubs==3.15.0
types-PyYAML==6.0.12.20260815

# Security scanning
bandit==1.9.4
